/**
 * Contains tests on repositories and on the associated models. In particular, for every model are defined:
 * <ul>
 *     <li>tests for the proper functioning of the CRUD operations implemented by the repository, like insertion, updating, deleting and reading of the data;</li>
 *     <li>tests for the proper functioning of the search operations implemented by the repository and the customized one;</li>
 *     <li>tests on the associations between the various models, in particular on cascade policies;</li>
 *     <li>tests that throw exceptions in case you try to insert data that violates the domain rules.</li>
 * </ul>
 */
package it.giorgiaauroraadorni.booktique.repository;
